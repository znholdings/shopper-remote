// Written for real at setup time as config.js. Anon key only - the
// service_role key must never appear in a page anyone can open.
window.SHOPPER_REMOTE_CONFIG = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-KEY",
  vapidPublicKey: "YOUR-VAPID-PUBLIC-KEY",
};
